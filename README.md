# Covid 19 - Serverless Lambda API

### Deploying to lambda

You need to setup your aws config before you can deploy to AWS. 

#### Install serverless from npm

`npm install serverless -g`

#### Deploy to lambda

`sls deploy`
